name: auto-vps

on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

jobs:
  setup-vps:
    runs-on: ubuntu-latest
    env:
      MEGA_EMAIL: ${{ secrets.MEGA_EMAIL }}
      MEGA_PASSWORD: ${{ secrets.MEGA_PASSWORD }}
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y tmate unzip openjdk-17-jre-headless python3-pip
          pip3 install mega.py

      - name: Start tmate session
        run: |
          mkdir -p links
          tmate -S /tmp/tmate.sock new-session -d
          tmate -S /tmp/tmate.sock wait tmate-ready
          tmate -S /tmp/tmate.sock display -p '#{tmate_ssh}' > links/ssh.txt
          cat links/ssh.txt

      - name: Download and setup Minecraft server
        run: |
          mkdir -p minecraft && cd minecraft
          wget https://api.papermc.io/v2/projects/paper/versions/1.20.1/builds/196/downloads/paper-1.20.1-196.jar -O server.jar
          echo "eula=true" > eula.txt
          echo -e "motd=GitHub VPS Minecraft Server\nmax-players=10\nonline-mode=false" > server.properties

      - name: Download plugins from MEGA
        run: |
          python3 mega_helper.py download_plugins plugins minecraft/plugins || echo "No plugins found or error"

      - name: Start Minecraft Server
        run: |
          cd minecraft
          nohup java -Xmx2G -Xms1G -jar server.jar nogui &

      - name: Backup world and SSH info
        run: |
          zip -r backup.zip minecraft links

      - name: Upload backup to MEGA
        run: |
          python3 mega_helper.py upload backup.zip /vps_backups/backup.zip

      - name: Keep VPS alive for 6 hours
        run: |
          echo "⏳ Keeping VPS alive for 6 hours..."
          sleep $((6 * 60 * 60))
